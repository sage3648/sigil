#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
JAR="$SCRIPT_DIR/compiler/build/libs/sigil.jar"
if [ ! -f "$JAR" ]; then
    echo "Building Sigil..."
    "$SCRIPT_DIR/gradlew" -p "$SCRIPT_DIR" :compiler:shadowJar -q
fi
java -jar "$JAR" "$@"
